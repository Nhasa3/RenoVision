[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -c \"import socket; print(socket.create_connection(('smtp.gmail.com', 587), 10))\""restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10