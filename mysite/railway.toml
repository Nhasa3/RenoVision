[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && python manage.py create_admin && python manage.py collectstatic --noinput && gunicorn mysite.wsgi"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10